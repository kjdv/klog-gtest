#pragma once

#include <string>

namespace klog_gtest {

// internal headers, only available inside this package (including the tets), go here

std::string private_function();

} // namespace klog_gtest
